name: Refresh fire data

on:
  schedule:
    # Daily static dumps — live NIFC/VIIRS stay fresh in the browser
    - cron: '0 15 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - run: node scripts/refresh-fire-data.mjs
      - run: node scripts/refresh-hotspots.mjs
      - run: node scripts/refresh-odot-closures.mjs
      - run: node scripts/refresh-evacuations-or.mjs
      - run: node scripts/refresh-shelters-or.mjs
      - uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: 'chore: refresh fire, hotspot, ODOT, evac, and shelter data'
          file_pattern: apps/web/public/data/fires.json apps/web/public/data/hotspots.json apps/web/public/data/odot-closures.json apps/web/public/data/evacuations-or.json apps/web/public/data/shelters-or.json
