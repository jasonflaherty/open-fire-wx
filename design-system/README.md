# Design System

Tokens for Open Fire WX.

- `tokens.css` — color, spacing, radius, motion
- `fonts.css` — Sora (display) + IBM Plex Sans (body)

Import from apps:

```ts
import '@openfirewx/design-system/tokens.css';
import '@openfirewx/design-system/fonts.css';
```
